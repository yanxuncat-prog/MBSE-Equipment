# 飞机设备数据管理系统 — 权限管理需求文档

> 版本 V1.0 | 2026-04-23 | 草案

---

## 1 文档概述

本文档定义飞机设备数据管理系统的权限管理需求。系统涵盖**构型、实物管控、布置、环境、重量、系统集成**六大业务模块，各模块数据字段由不同专业团队负责维护。

**权限设计核心原则：**

- 各专业人员仅能编辑本专业归属字段，同时可查看全局设备基本信息。
- 设备行的增删由构型管理员统一管控。
- 专业负责人和 Admin 可赋予他人权限。

---

## 2 角色定义

系统定义 5 种角色，权限从高到低排列：

### 2.1 系统管理员（Admin）

- 最高权限，可执行所有操作：用户管理、角色分配、全局配置、数据导入导出、系统日志查看。
- 可向任何人赋予或收回任意角色。
- 通常由 IT 管理团队或项目总负责人担任。

### 2.2 构型管理员（ConfigManager）

- 负责设备清单的**行级管理**：设备条目的新增、删除、启用、停用。
- 拥有构型模块所有字段的读写权限。
- **不能**自行赋予他人权限，需通过 Admin 操作。

### 2.3 专业负责人（DomainLead）

- 各专业方向的负责人。
- 拥有**本专业所属字段**的读写权限。
- 可管理本专业下属人员的权限分配（授予/收回本专业的读或写权限）。
- 可查看（只读）其他专业的字段数据。

### 2.4 专业人员（DomainUser）

- 各专业方向的具体操作人员。
- 经专业负责人或 Admin 授权后，可编辑**本专业归属字段**。
- 可查看设备基本信息和其他专业字段（只读）。
- 不同专业人员之间**不可交叉编辑**。

### 2.5 只读用户（Viewer）

- 仅具备全局数据的查看权限，不可进行任何编辑操作。
- 适用于项目评审专家、外部供应商代表等。

---

## 3 业务模块与字段归属

### 3.1 构型模块

- **归属方**：构型管理团队
- **字段**：
  - 设备基本信息：ATA 章节（`system_ata`）、设备名称（`device_name`，含中文/英文/缩写）、设备编号（`device_code`）、LIN 号（`lin_no`，通常 9 位，前两位与 ATA 匹配）、DM 号（`dm_no`，通常 14 位）
  - 架次适用性（`aircraft_applicability`）：0 号机 / 1 号机 / 2 号机 / X 号机
  - 构型分类（`config_category`）：1 / 2 / 3 / 4
  - 产品类别（`product_type`）：货架产品 / 新研产品 / 货架产品更改 — **由各系统专业人员协同填写**
  - 是否为 TSO 件（`is_tso`）：是 / 否 — **由各系统专业人员协同填写**

### 3.2 实物管控模块

- **归属方**：各系统专业 + 总装团队
- **字段**：
  - 安装分工（`install_responsibility`）：上飞公司 / 总装团队
  - 现场结构具备安装条件（`structure_ready`）：是 / 否
  - 现场系统具备安装条件（`system_ready`）：是 / 否 — **由对应系统专业人员填写**
  - 设备到货日期（`delivery_date`）
  - 计划安装日期（`planned_install_date`）
  - 设备实物状态（`physical_status`）
  - 是否已编制 AO（`has_ao`）：是 / 否

### 3.3 布置模块

- **归属方**：布置专业
- **字段**：
  - 设备布置区域（`layout_zone`）：按节-段编号分类，参见布置区域编码表
  - MICD 接口确认状态（`micd_status`）：已有备忘录 / 签审中 / 已发布 / 暂无 MICD
  - 数模发布状态（`model_status`）：占位数模 / 签审中 / 已发布 / 暂无数模
  - 设备尺寸（`device_size`）：X / Y / Z 三轴，单位 mm

### 3.4 环境模块

- **归属方**：环境专业
- **字段**：
  - 电搭接方式（`bonding_method`）：面搭接 / 线搭接 / 紧固件搭接 / 连接器引线接地 / 不适用
  - 电搭接类型（`bonding_type`）：天线搭接 A 类 / 电流回路搭接 C 类 / 易燃危险区搭接 HE 类 / 防射频干扰搭接 R 类 / 防电击搭接 H 类 / 静电防护搭接 S 类 / 闪电防护搭接 L 类 / 设施接地 M 类
  - 电搭接阻值要求（`bonding_resistance`）
  - DO-160 环境鉴定相关字段（第 4~26 章各章节的鉴定等级与试验状态）

### 3.5 重量模块

- **归属方**：重量专业
- **字段**：
  - 重量（`weight_kg`）：含设备托架，湿重，单位 kg
  - 重心坐标（`cg_coord_mm`）：全机坐标系，单位 mm
  - 惯量（`inertia_kg_m2`）：6 个分量，单位 kg·m²
  - 重量指标（`weight_target`）：由 PACE 提取
  - 超重风险（`overweight_risk`）：低 / 中 / 高
  - **注**：设备实际重量由各系统专业人员录入，重量指标和超重风险由重量专业统一评估填写

### 3.6 系统集成模块

- **归属方**：系统集成专业
- **字段**：
  - 是否有 EICD（`has_eicd`）：是 / 否
  - 是否为电子电气设备（`is_electrical`）：是 / 否
  - 供电方式（`power_supply_method`）
  - 供电余度（`power_redundancy`）
  - 供电电压（`power_voltage`）
  - 用电功率（`power_consumption`）
  - 设备壳体材料（`shell_material`）：金属 / 复材
  - 负载类型（`load_type`）

### 3.7 通用字段（各专业共用）

- 设备联系人（`contact_person`）
- 备注（`remarks`）
- 最后更新日期（`last_updated`）— 操作修改后自动记录
- 更新说明（`update_notes`）— 按次填写更新记录

---

## 4 权限矩阵

`W` = 读写，`R` = 只读，`✕` = 无权限

| 功能模块 | 数据字段/操作 | Admin | ConfigManager | DomainLead | DomainUser | Viewer |
|---------|-------------|-------|---------------|------------|------------|--------|
| 构型 | 设备基本信息（ATA/名称/编号/LIN/DM） | W | W | R | R | R |
| 构型 | 架次适用性 / 构型分类 | W | W | R | R | R |
| 构型 | 产品类别 / 是否TSO件 | W | W | R | W† | R |
| 构型 | **新增/删除设备行** | W | W | ✕ | ✕ | ✕ |
| 实物管控 | 安装分工 | W | R | W‡ | W‡ | R |
| 实物管控 | 现场结构具备安装条件 | W | R | W‡ | W‡ | R |
| 实物管控 | 现场系统具备安装条件 | W | R | W‡ | W‡ | R |
| 实物管控 | 设备到货日期 / 计划安装日期 | W | R | W‡ | W‡ | R |
| 布置 | 设备布置区域 / MICD / 数模状态 / 尺寸 | W | R | W | W | R |
| 环境 | 电搭接（方式/类型/阻值） | W | R | W | W | R |
| 环境 | DO-160鉴定等级与试验状态 | W | R | W | W | R |
| 重量 | 重量/重心/惯量/超重风险/重量指标 | W | R | W | W | R |
| 系统集成 | 负载电气特性（电压/功率等） | W | R | W | W | R |
| 系统集成 | EICD接口确认 / 是否电设备 / 壳体材料 | W | R | W | W | R |
| 通用 | 备注 / 更新说明 | W | R | W | W | R |
| 通用 | 查看操作日志 | W | W | W | R | ✕ |
| 通用 | **用户权限管理** | W | ✕ | 本专业 | ✕ | ✕ |

**标注说明：**
- `W†` = 仅限本系统专业人员填写**本系统设备**的该字段
- `W‡` = 仅限负责该系统的专业人员编辑**对应系统**的字段行
- DomainLead / DomainUser 的 `W` 权限均限定于**本专业归属字段**，对其他专业字段为 `R`

---

## 5 行级操作权限

### 5.1 新增设备行

- 操作角色：仅 Admin、ConfigManager。
- 必填字段：ATA 章节、设备名称、设备编号、LIN 号。
- 其余字段可在创建后由各专业人员补充。
- 系统自动记录：操作人、操作时间。

### 5.2 删除设备行

- 操作角色：仅 Admin、ConfigManager。
- 采用**软删除**机制：标记为"已删除"状态，不做物理删除。
- 删除后的设备进入"已删除设备"视图，可由 Admin 恢复。
- 系统自动记录：操作人、操作时间、删除原因。

### 5.3 锁定设备行

- 操作角色：ConfigManager、Admin。
- 锁定后任何角色（包括有编辑权限的专业人员）均不可编辑该行任何字段，仅可查看。
- 典型场景：已通过评审、进入冻结状态的设备数据。
- 解锁需由 ConfigManager 或 Admin 操作。

---

## 6 权限分配与审批流程

### 6.1 权限赋予规则

| 操作者 | 可分配的角色/权限范围 |
|-------|-------------------|
| Admin | 任意角色，包括提升为 ConfigManager 或 DomainLead |
| DomainLead | 本专业范围内为下属人员赋予/收回读、写权限 |
| ConfigManager | 不能分配权限，需通过 Admin |
| DomainUser | 无权限管理能力 |
| Viewer | 无权限管理能力 |

### 6.2 权限变更流程

```
申请人提交权限申请（所需角色 + 理由）
       ↓
审批人审核（Admin 或对应 DomainLead）
       ↓
批准 → 系统生效 + 记录变更日志
拒绝 → 通知申请人 + 记录拒绝原因
```

变更日志包含：变更人、变更时间、变更内容、审批人。

### 6.3 权限回收

- 人员调离、项目结束或职责变更时应及时回收权限。
- 建议系统提供权限到期提醒功能。
- 定期进行权限审计（建议周期：每季度一次）。

---

## 7 数据隔离与约束规则

### 7.1 专业隔离原则

不同专业人员之间**不可交叉编辑**字段。

- 前端：隐藏或禁用无权限的编辑入口（按钮置灰/隐藏、输入框 readonly）。
- 后端：接口层进行二次鉴权，校验当前用户的专业归属与目标字段的归属是否匹配。
- 示例：布置专业人员不能修改重量模块字段；系统集成人员不能修改环境模块字段。

### 7.2 系统级数据隔离（按 ATA 章节）

对于按系统（ATA 章节）划分的字段，各系统专业人员仅能编辑**本系统设备**的数据。

- 实现方式：每个用户绑定其负责的 ATA 章节列表，编辑时校验设备的 `system_ata` 是否在该列表中。
- 示例：航电系统人员不能修改环控系统设备的供电参数。

### 7.3 构型锁定约束

当设备进入特定构型阶段时（如评审冻结、首飞状态锁定），对相关字段实行整体锁定。

- 锁定状态下，即使有编辑权限也不可修改。
- 需由 ConfigManager 或 Admin 解锁后方可操作。
- 锁定粒度：支持按行锁定、按构型批量锁定。

---

## 8 审计与日志

系统应记录所有数据变更操作的完整日志：

| 日志字段 | 说明 |
|---------|------|
| `operator` | 操作人（用户名 + 角色） |
| `timestamp` | 操作时间（精确到秒） |
| `action_type` | 操作类型：CREATE / UPDATE / DELETE / LOCK / UNLOCK |
| `target` | 操作对象：设备编号 + 字段名 |
| `old_value` | 变更前的值 |
| `new_value` | 变更后的值 |

**日志访问权限：**
- Admin / ConfigManager：可查看完整日志。
- DomainLead：可查看本专业相关的操作日志。
- DomainUser：仅可查看自己的操作日志。
- Viewer：无日志查看权限。

**约束：** 日志不可被任何角色删除或修改。

---

## 9 接口与扩展需求

### 9.1 外部系统对接

权限系统需预留以下对接接口：

| 外部系统 | 权限校验需求 |
|---------|------------|
| EWIS 数据管理系统（DOORS） | 数据同步时校验操作人员是否有目标字段的写权限 |
| 负载统计系统 | 拉取设备用电参数时按系统集成专业权限过滤 |
| 重量管理系统 | 同步重量指标和超重评估结果 |

### 9.2 扩展性要求

- 支持未来新增专业模块和自定义字段时，灵活扩展权限模型。
- 权限配置应数据驱动（角色-模块-字段映射关系存储在数据库），而非硬编码。
- 新增专业模块时，仅需在权限配置表中添加映射关系，无需修改核心代码。

---

## 10 数据模型参考（供开发参考）

### 10.1 核心表结构

```
users
├── user_id (PK)
├── username
├── role: enum(Admin, ConfigManager, DomainLead, DomainUser, Viewer)
├── domain: 所属专业（布置/环境/重量/系统集成/构型/实物管控）
└── ata_chapters[]: 负责的ATA章节列表（用于系统级数据隔离）

permissions
├── permission_id (PK)
├── user_id (FK → users)
├── module: 业务模块
├── field_group: 字段组
├── access_level: enum(READ, WRITE)
├── granted_by: 授权人 user_id
└── granted_at: 授权时间

device_rows
├── device_id (PK)
├── system_ata
├── device_name / device_code / lin_no / dm_no
├── is_locked: boolean
├── locked_by: user_id
├── is_deleted: boolean (软删除)
├── deleted_by: user_id
├── created_by / created_at
└── ... (各模块字段)

audit_logs
├── log_id (PK)
├── operator (user_id)
├── timestamp
├── action_type: enum(CREATE, UPDATE, DELETE, LOCK, UNLOCK)
├── device_id (FK)
├── field_name
├── old_value / new_value
└── (不可删除/修改)
```

### 10.2 权限校验伪逻辑

```
function canEdit(user, device, field):
    // 1. Admin 拥有所有权限
    if user.role == Admin: return true

    // 2. 检查行锁定
    if device.is_locked: return false

    // 3. 检查软删除
    if device.is_deleted: return false

    // 4. Viewer 无编辑权限
    if user.role == Viewer: return false

    // 5. 行级操作（增删）仅限 ConfigManager
    if field == ROW_OPERATION:
        return user.role == ConfigManager

    // 6. 构型模块字段：仅 ConfigManager 可写
    if field.module == "构型" and field not in ["product_type", "is_tso"]:
        return user.role == ConfigManager

    // 7. 字段级权限：检查专业归属
    if field.module != user.domain: return false

    // 8. 系统级隔离：检查 ATA 章节
    if field.requires_ata_check:
        if device.system_ata not in user.ata_chapters: return false

    // 9. 检查用户是否已被授权该字段的写权限
    return hasPermission(user, field.module, field.group, WRITE)
```

---

## 11 权限模型总结

本系统权限模型为 **RBAC + 字段级 + 行级** 三层控制：

1. **RBAC（基于角色的访问控制）**：通过 Admin、ConfigManager、DomainLead、DomainUser、Viewer 五种角色控制功能级别的访问。
2. **字段级权限**：各专业模块的字段仅对归属专业的人员开放编辑权限，其他专业人员只读。
3. **行级权限**：设备行的增删由 ConfigManager 统一管控，并支持行锁定机制。

该模型确保了数据的安全性和专业分工的明确性，同时通过权限委托机制（DomainLead 可管理本专业人员权限）实现了管理的灵活性。
